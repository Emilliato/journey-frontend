export const environment = {
  production: true,
  // Deployed API. HTTPS is required — the production site is served over
  // HTTPS from journey.co.za, so an HTTP or bad-cert API would be blocked as
  // mixed content. The :448 port must be reachable from clients' networks.
  apiUrl: 'https://learnbridgeapi.adaptivesystems.co.za:448',
};
